services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ai_builder
    ports:
      - '5432:5432'

  backend:
    build: ./backend
    ports:
      - '8080:8080'
    depends_on:
      - postgres
